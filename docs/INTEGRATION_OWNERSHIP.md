# Integration and release ownership

Verified September 9, 2026 from repository source, GitHub workflow configuration and recent run status. No secret values were read. GitHub account `TomEnglish` is the repository owner and technical escalation contact; a separate business/source-data owner has not been designated.

| Integration | Operating repository and entry point | Credentials | Schedule and observed state | Failure response |
| --- | --- | --- | --- | --- |
| MSR site | `TomEnglish/invenio-tek-msr`, Netlify site `bfd78caa-be12-4ef6-9272-1c0a113e3c15` | Existing Netlify GitHub integration; no production secrets in quality tests | Main-branch Git deploy; CI waits for the exact commit and checks 12 asset hashes plus 7 excluded private paths | Check Quality and release checks and Netlify build logs. Repository owner investigates. Restore the last verified Netlify deploy if needed; do not bypass RLS. |
| Field application | `TomEnglish/invenio-field-platform`, EAS project `2628e94e-6c96-4cd3-b753-3f859707a23d` | Existing local EAS authorization; public Supabase config injected at release | EAS production update is an explicit release. CI only builds and tests with dummy credentials; no unattended native or OTA publish | Check Field quality checks; verify runtime/channel compatibility and device update ID before treating publication as uptake. |
| PO and shipment workbook | Canonical MSR `sync_po_shipment_data.py` → `import_po_shipment_snapshot` | `SUPABASE_URL`, server-only `SUPABASE_SERVICE_ROLE_KEY`, explicit `--project-id`; local operator environment | Manual validation by default; `--apply` required. Current workbook rejected at PO Parts Log H71 and has duplicate shipment IDs. No scheduled import | Repository owner coordinates corrected source workbook. Do not silently deduplicate or retry unvalidated data. The database transaction leaves existing records intact on failure. |
| Samsara GPS | **Still operating in `TomEnglish/MSR_F6B`**, workflow `Sync Samsara Data` | Secret names present there: `SAMSARA_API_TOKEN`, `SUPABASE_URL`, `SUPABASE_ANON_KEY`. Values and destination were not inspected | Cron `0 * * * *`; latest observed September 9 10:22 UTC run succeeded. Canonical MSR has no repository secrets and its manual workflow now explicitly fails with handover instructions | Repository owner checks the legacy run and destination. A successful old job does not prove the shared MSR database is current; its last observed GPS import is February 22. Keep the legacy repository until cutover is verified. |
| Ready-by dates | Canonical MSR contains legacy `sync_delivery_dates.py`; no GitHub workflow or scheduler found in this repository | Legacy code expects anonymous key and performs delete/reinsert | Not approved for the current project-scoped database. No execution performed | Replace with validated, project-scoped, server-only transactional import before use. Repository owner must confirm source owner and cadence. |
| Installation snapshots | Canonical MSR, project-scoped `installation_datasets`, migration 017 | Database release operator; authenticated users read assigned projects | Existing snapshots loaded September 9; no refresh scheduler found. Load timestamp is not source currency | Source owner/cadence still need confirmation before scheduling refreshes. |
| Auth email | Shared Supabase Auth → Resend SMTP, verified `unleashingflow.com` sender | Resend key stored in Supabase SMTP configuration; never browser code | Event-driven invitations/recovery; delivery already verified to the owner's two addresses | Check Supabase Auth and Resend delivery logs. Invitation acceptance still needs the intended user; delivery alone is not account acceptance. |

## Samsara cutover prerequisites

Confirm the destination project and ownership of each tracker before configuring any new writer. Replace the legacy anonymous-key writes with an explicitly project-scoped server-side path; validate location/history behavior, timeouts and duplicate handling. Configure secrets in the canonical repository through its secret store, test manually against a staging project, and compare counts and freshness. Disable the old schedule immediately before enabling the replacement, verify successive successful runs and alerts, then consider archival separately. The current task does not retire repositories or enable a duplicate writer.

## Reproducing quality checks

MSR: `node --test tests/*.test.js`, the Deno commands in `.github/workflows/quality.yml`, `python -m unittest discover -s tests -p '*_test.py'` after installing `requirements-import.txt`, and `bash tests/database/run.sh` with PostgreSQL binaries on PATH. CI checks out the immutable Field baseline `a3515a24a4c976b6afb6b17aadb95b71da17d9dd` for migrations 001–011. Set `FIELD_REPO_PATH` to another baseline explicitly when testing a planned shared-schema change; update the pinned CI reference in that release.

The build generates `release.json` containing only the commit and public asset hashes. `node scripts/smoke-deploy.mjs <site-origin> <full-commit-sha> [attempts]` checks identity, contents and exclusions. Production smoke runs after main-branch quality checks, waiting up to 90 attempts for Netlify. Re-run a failed job after a transient deployment failure; restoring an older release deliberately fails the expected-commit check.

Field: `npm ci`, `npm run lint`, `node --test tests/*.test.cjs`, and `npx expo export --platform all --output-dir dist-ci`. CI does not require Supabase/EAS secrets and makes no live database changes.

Sources: [GitHub workflow events](https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows), [checkout multi-repository usage](https://github.com/actions/checkout), [Netlify build commit variable](https://docs.netlify.com/configure-builds/environment-variables).

Netlify's existing Pretty URLs setting rewrote HTML anchors after the build, causing the first exact-asset smoke check to fail as intended. File-based configuration now disables that rewrite. Deploy Previews can additionally inject Netlify's feedback drawer; exact-byte smoke is intended for production and unmodified deploys, not previews with an injected drawer. See [Netlify post-processing configuration](https://docs.netlify.com/build/configure-builds/file-based-configuration/).
